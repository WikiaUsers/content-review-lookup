/* Any JavaScript here will be loaded for all users on every page load. */
importArticles({
    type: 'script',
    articles: [
        // other scripts
        'u:dev:QuickDelete/multiCats.js',
        // other scripts
    ]
});