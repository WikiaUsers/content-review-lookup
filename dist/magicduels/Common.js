/* Any JavaScript here will be loaded for all users on every page load. */
var tooltips_list = [
    {
        classname: 'card-image-tooltip',
        parse: '[[File:<#parameter#>.png|size=160px|link=]]',
        delay: 100
    }
]

var tooltips_config = {
    offsetX: 8,
    offsetY: 8,
    waitForImages: true,
    /*events: ['CustomEvent'],*/
};