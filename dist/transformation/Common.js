/* Any JavaScript here will be loaded for all users on every page load. */

/* ReferencePopups: Disable open/close animations */

((window.dev = window.dev || {}).ReferencePopups = dev.ReferencePopups || {}).defaults = { animate: false };