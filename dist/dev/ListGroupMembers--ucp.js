/**
 * ListGroupMembers
 * 
 * A script that loads a list of all users
 * in a specific (canonical or custom) user group
 **/
( function( window, $, mw ) { 
    "use strict";
} )( this, jQuery, mediaWiki );