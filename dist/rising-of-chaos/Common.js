/* Any JavaScript here will be loaded for all users on every page load. */
.noautonum .tocnumber { display: none; }

importArticles({
    type: 'script',
    articles: [
        'u:dev:NoLicenseWarning.js'
    ]
});

importArticles({
    type: 'script',
    articles: [
        'u:dev:MediaWiki:BackToTopButton/code.js',
    ]
});