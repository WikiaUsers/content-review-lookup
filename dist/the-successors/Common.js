/* Any JavaScript here will be loaded for all users on every page load. */
 
 SpoilerAlert = {
  categories: "Proleans",
}
importArticles({
    type: "script",
    articles: [
        "w:c:dev:SpoilerAlert/code.2.js"
    ]
});
 