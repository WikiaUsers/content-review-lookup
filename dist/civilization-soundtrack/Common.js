/* Any JavaScript here will be loaded for all users on every page load. */
importScript("MediaWiki:Reports.js");
 
    // Civilization Wiki topic interwiki links:

 $("#CivWikiLink").prependTo(".page-header__contribution > div:first-child").css({"display": "inline-block"});

});