/* Bất kỳ mã JavaScript ở đây sẽ được tải cho tất cả các thành viên khi tải một trang nào đó lên. */
// Display 12 hour time followed by day, month (English, full name)
// and year with "(WIB)" at the end
importArticles({
    type: 'script',
    articles: [
        'u:dev:UTCClock/code.js',
    ]
});