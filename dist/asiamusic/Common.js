/* Any JavaScript here will be loaded for all users on every page load. */

window.massCategorizationDelay = 2000;
importArticles({
    type: 'script',
    articles: [
        'u:dev:MediaWiki:MassRename/code.js',
    ]
});