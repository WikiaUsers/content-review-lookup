window.batchDeleteDelay = 100;

window.MassEditConfig = {
  interval: 100,
  placement: {
    element: "toolbar",
    type: "append",
  }
};

window.tooltips_config = {
    offsetX: 5,
    offsetY: 10,
    waitForImages: true,
};