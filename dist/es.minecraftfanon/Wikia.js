// Otros

importArticles({
    type: "script",
    articles: [
        "w:c:dev:EditBoardDescription/code.js"
    ]
});

importArticles({
    type: 'script',
    articles: [
        'w:c:dev:AutoEditDropdown/code.js'
    ]
});

importArticles({
    type: "script",
    articles: [
        // Horario "MediaWiki:Wikia.js/Horario.js", {no disponible}
        // Slider img
        "MediaWiki:Wikia.js/Slider.js",
    ]
});

// Script de Countdown
importScriptPage('Countdown/code.js', 'dev');

// Resultado de busqueda

importArticles({
    type: 'script',
    articles: [
        'u:dev:SearchSuggest/code.js'
    ]
});