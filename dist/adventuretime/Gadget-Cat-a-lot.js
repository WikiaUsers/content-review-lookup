window.catALotPrefs = {
    watchlist: 'nochange',       // Don't alter watchlist
    minorEdit: true,             // Mark edits as minor
    editSummary: 'Mass-categorizing files via Cat-a-lot',
    defaultAction: 'add',        // Default mode = Add to category
    enableMove: true,            // Allow moving between categories
    enableRemove: true,          // Allow removing categories
    autoCommit: true,            // Skip confirmation dialog
    useServerAPI: true,          // Faster API edits
    markPages: true              // Highlight pages after action
};