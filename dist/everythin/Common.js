/* Any JavaScript here will be loaded for all users on every page load. */
 
//Last Edited by code
 window.lastEdited = {
    avatar: true,
};
importArticles({
    type: 'script',
    articles: [
        'u:dev:LastEdited/code.js'
    ]
});