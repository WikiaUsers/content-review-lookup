/* Any JavaScript here will be loaded for all users on every page load. */

window.tooltips_list = [
    {
        classname: 'characterdisplay-tooltip',
        text: "<#text#>"
    }
]

window.tooltips_config = {
    noCSS: false,
    offsetX: 5,
    offsetY: 10,
    waitForImages: true,
}