/* Any JavaScript here will be loaded for all users on every page load. */
switch (mw.config.get('wgJackWalten')) {
    case 'Jack Walten':
var wiki_names = ["Pick Up The Phone, Now.", "Let Me Out Of Here"];        break;