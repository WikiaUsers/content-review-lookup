/* Any JavaScript here will be loaded for all users on every page load. */
$(function() {
	if(skin == "oasis") {
		var $sidebar = $('.WikiaPageHeader h1');
		var comboString ='<br />';
			$sidebar.append(comboString);
		}
	}