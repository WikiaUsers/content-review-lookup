$(function () { /* Runs when the page loads. */
  if (wgSiteName == "Sonako back up Wiki") { /* Checks that this script is being run on the Saints Row Wiki */
    if (typeof doMagic == "function") doMagic(); /* This does the magic. */
  }
});