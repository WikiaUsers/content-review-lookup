            /**
             * Add imports to an array
             */

            // scripts to be loaded on (nearly) every pageview
            scripts.push(
                // Konami code
                'MediaWiki:Common.js/Konami.js'
            );