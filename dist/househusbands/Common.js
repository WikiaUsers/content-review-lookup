/* Any JavaScript here will be loaded for all users on every page load. */
window.UserBadgesJS = {
	inactive: 30, // No edits in 30 days = inactive badge
	
importArticle({type:'script', article:'w:c:dev:UserBadges/code.js'});