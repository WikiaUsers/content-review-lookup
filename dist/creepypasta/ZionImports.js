/*
    Zion is an alternative script loader which allows the use of ES6
    Wikia's parser for minifying JavaScript before load doesn't support modern JS
*/

$(function () {
    ZION.load([
        'MediaWiki:Showcase.js'
    ]);
});