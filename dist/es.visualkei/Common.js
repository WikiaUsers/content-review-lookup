/* Cualquier código JavaScript escrito aquí se cargará para todos los usuarios en cada carga de página. */
switch (mw.config.get('wgPageName')) {
    case 'Próximos Lanzamientos':
        // JS here will be applied to "page name"
        break;
}