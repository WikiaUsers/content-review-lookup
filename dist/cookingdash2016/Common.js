/* Any JavaScript here will be loaded for all users on every page load. */

var tooltips_config = {
    offsetX: 5,
    offsetY: 10,
    waitForImages: true,
    events: ['CustomEvent'],
    noCSS: true,
}