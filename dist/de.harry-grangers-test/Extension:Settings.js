/**
* Extension:Settings
* Makes additional settings editable on a GUI
*/

function Settings() {
    this.description = 'Makes additional settings editable on a GUI';

    this.install = function() {
        super();
        console.log('installed');
    }

    this.get = function(user) {
        if(arguments.length == 1) {

        }
        elseif(arguments.length == 2) {

        }
    }
}