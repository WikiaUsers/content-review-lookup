/* Any JavaScript here will be loaded for all users on every page load. */
console.log(test);
window.UserTagsJS = {
	modules: {},
	tags: {
		bureaucrat: { u:'SuperAdmin' },
		blocked: { u: 'Banned' }
	}
};