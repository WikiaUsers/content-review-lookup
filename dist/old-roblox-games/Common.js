/* Any JavaScript here will be loaded for all users on every page load. */
console.log(test);
window.UserTagsJS = {
	modules: {},
	tags: {
		bureaucrat: { u:'THE ELDER ONE' },
		blocked: { u: 'Banned' }
	}
};